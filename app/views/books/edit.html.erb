<h1>Editing Book</h1>
<%= form_with model:@book, url:"/books/#{@book.id}", local: true do |f| %>
<!--error message-->
  <% if @book.errors.any? %>
    <div class="form-error">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>
      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
   <!--post form-->
  <div class= "post">
    <%= f.label :title %>
    <br><%= f.text_field :title %></br>
    <br><%= f.label :body %><br>
    <%= f.text_area :body %>
    <div class=" button"><%= f.submit 'Update Book' %></div>
<% end %>
  </div>

<div class= "foot">
 <%= link_to "show", "/books/#{@book.id}" %></td>
  <div class= "back">
    <%= link_to "Back", "/books" %>
  </div>
</div>

